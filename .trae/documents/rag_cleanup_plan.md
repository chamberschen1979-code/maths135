# RAG 出题方案文件清理与重构计划

## 📊 M04.json 题目统计

### 各变例各难度题目数量

| 变例              | L2     | L3     | L4     | 总计      |
| --------------- | ------ | ------ | ------ | ------- |
| **V1: 幂指对代数韧性** | <br /> | <br /> | <br /> | <br />  |
| 1.1 复合运算与指对转化   | 7      | 18     | 2      | 27      |
| 1.2 指对同构        | 2      | 8      | 13     | 23      |
| **V2: 图象特征与性质** | <br /> | <br /> | <br /> | <br />  |
| 2.1 定点、对称性与反函数  | 12     | 14     | 2      | 28      |
| 2.2 复合单调性与值域    | 9      | 14     | 11     | 34      |
| **总计**          | **30** | **54** | **28** | **112** |

### 标杆题 vs 普通题

| 类型                  | 数量    | 说明             |
| ------------------- | ----- | -------------- |
| `original_pool`     | 112 题 | 主题目池           |
| `master_benchmarks` | 8 题   | 标杆题（用于显示等级状态灯） |

**注意**：合并后，标杆题和普通题都被放入了 `original_pool`，通过 `source_type` 字段区分来源：

* `source_type: "original_pool"` - 来自旧版的普通题池（60 题）

* `source_type: "master_benchmarks"` - 来自旧版的标杆题库（10 题）

* 无 `source_type` 标记 - 新版原有的题目（40 题）

### JSON vs MD 文件对比

| 文件                  | 题目数量                       |
| ------------------- | -------------------------- |
| `M04.json`          | 112 题                      |
| `M04_Master_RAG.md` | 0 题（Markdown 格式问题，统计方法需修正） |

**问题**：MD 文件中题目格式为 `**题目（来源）**:`，但统计脚本使用 `**题目（` 匹配，可能因编码问题未正确统计。

***

## 📋 分析目标

分析整个项目中与出题相关的文件，检查是否有残留的无用文件需要清理，确保与新的 RAG 出题方案兼容。

***

## 📁 文件分类分析

### 一、核心出题文件（必须保留，已适配 RAG）

| 文件                                    | 状态    | 说明                                                            |
| ------------------------------------- | ----- | ------------------------------------------------------------- |
| `src/utils/dataLoader.js`             | ✅ 已适配 | 新增 RAG 函数：`extractQuestionsByLevel`, `selectQuestionFromPool` |
| `src/utils/problemLogic.js`           | ✅ 已适配 | `selectBenchmark`, `selectSeedQuestion` 已支持 RAG 模式            |
| `src/utils/promptBuilder.js`          | ✅ 已适配 | 新增保真改写策略、动态防御约束                                               |
| `src/services/questionVerifier.js`    | ✅ 已适配 | 新增通用合规性检查函数                                                   |
| `src/components/WeeklyMissionNew.jsx` | ✅ 已适配 | 数据透传正常                                                        |

***

### 二、配置文件（需要检查）

| 文件                               | 状态      | 问题                                               | 建议                  |
| -------------------------------- | ------- | ------------------------------------------------ | ------------------- |
| `src/config/difficultyConfig.js` | ⚠️ 部分使用 | 定义了 L1-L4 难度配置，但 RAG 模式下难度由 JSON 中的 `level` 字段决定 | **保留**：ELO 换算逻辑仍需使用 |
| `src/config/syllabusRules.js`    | ✅ 保留    | 年级限制、超纲内容检查                                      | **保留**：仍需使用         |

***

### 三、数据文件（需要清理）

| 文件                               | 状态     | 问题                                                | 建议                       |
| -------------------------------- | ------ | ------------------------------------------------- | ------------------------ |
| `src/data/M04.json`              | ✅ 已升级  | 112 题，RAG 格式                                      | **保留**                   |
| `src/data/M04_Master_RAG.md`     | ✅ 已升级  | Markdown 题库                                       | **保留**                   |
| `src/data/M01.json` - `M17.json` | ⚠️ 旧格式 | 仍包含 `variable_knobs`, `level_constraints` 等生成控制字段 | **待升级**：其他母题需要按 M04 方式重构 |
| `src/data/strategy_lib.json`     | ⚠️ 待确认 | 杀手锏武器库                                            | **保留**：武器 ID 仍在使用        |
| `src/data/tacticalMaps.json`     | ⚠️ 待确认 | 战术地图                                              | **检查后决定**                |
| `src/data/scoringEngine.json`    | ⚠️ 待确认 | 评分引擎                                              | **检查后决定**                |

***

### 四、验证器文件（需要检查）

| 文件                                          | 状态     | 问题             | 建议                       |
| ------------------------------------------- | ------ | -------------- | ------------------------ |
| `src/services/verifiers/M04_exp_log_iso.js` | ⚠️ 硬编码 | 包含 M04 特定的验证逻辑 | **保留但需重构**：改为从 JSON 读取约束 |
| `src/services/verifiers/M03_*.js`           | ⚠️ 硬编码 | 其他母题的验证器       | **待检查**                  |
| `src/services/verifiers/M05_*.js`           | ⚠️ 硬编码 | 其他母题的验证器       | **待检查**                  |
| `src/services/verifiers/M06_*.js`           | ⚠️ 硬编码 | 其他母题的验证器       | **待检查**                  |

***

### 五、工具文件（需要检查）

| 文件                               | 状态      | 问题          | 建议                                  |
| -------------------------------- | ------- | ----------- | ----------------------------------- |
| `src/utils/motifWeaponMapper.js` | ⚠️ 部分使用 | 硬编码母题到武器的映射 | **保留但需重构**：优先从 JSON 读取 `weapons` 字段 |
| `src/utils/weaponUtils.js`       | ⚠️ 待确认  | 武器工具函数      | **检查后决定**                           |
| `src/utils/weaponHelper.js`      | ⚠️ 待确认  | 武器辅助函数      | **检查后决定**                           |

***

### 六、临时/备份文件（可删除）

| 文件                                           | 状态   | 建议               |
| -------------------------------------------- | ---- | ---------------- |
| `src/data/M04_backup_20260325.json`          | 备份   | **可删除**（已有旧版备份）  |
| `src/data/M04_Master_RAG_backup_20260325.md` | 备份   | **可删除**          |
| `src/data/clean_and_convert_m04.py`          | 临时脚本 | **可删除**          |
| `src/data/fix_m04.py`                        | 临时脚本 | **可删除**          |
| `src/data/refactor_m04.py`                   | 临时脚本 | **可删除**          |
| `src/data/query_demo.py`                     | 演示脚本 | **可保留**：RAG 查询演示 |
| `migrate_m04_data.py`                        | 临时脚本 | **可删除**          |
| `merge_m04_full.py`                          | 临时脚本 | **可删除**          |

***

## 🔧 需要修改的文件

### 1. `src/utils/problemLogic.js`

**问题**：仍引用 `variable_knobs` 和 `level_constraints`

**修改方案**：

* `extractLevelConstraints` 函数：改为从 JSON 的 `meta` 字段读取约束

* `prepareGenerationContext` 函数：改为从 RAG 数据读取约束

* 保留向后兼容：如果 JSON 中没有 `variable_knobs`，则使用 `meta` 字段

### 2. `src/utils/motifWeaponMapper.js`

**问题**：硬编码母题到武器的映射

**修改方案**：

* 添加注释：优先从 JSON 的 `meta.weapons` 字段读取

* 保留作为后备映射

### 3. `src/services/verifiers/M04_exp_log_iso.js`

**问题**：硬编码 M04 特定的验证逻辑

**修改方案**：

* 添加函数 `getConstraintsFromJSON(motifData, level)` 从 JSON 读取约束

* 保留核心验证逻辑，但约束参数改为动态获取

***

## 📊 执行计划

### 阶段 1：清理临时文件（低风险）

1. 删除 `src/data/` 下的临时 Python 脚本
2. 删除备份文件（可选）

### 阶段 2：修改核心逻辑文件（中风险）

1. 修改 `problemLogic.js`：移除对 `variable_knobs` 的硬编码依赖
2. 修改 `motifWeaponMapper.js`：添加优先从 JSON 读取的注释

### 阶段 3：重构验证器文件（高风险，可选）

1. 修改 `M04_exp_log_iso.js`：约束参数改为动态获取
2. 其他验证器暂不修改（等其他母题升级后再统一处理）

***

## ⚠️ 注意事项

1. **其他母题数据文件（M01-M17）**：目前仍使用旧格式，需要逐步按 M04 方式重构
2. **向后兼容**：修改时需要保留对旧格式的支持，直到所有母题都升级完成
3. **测试验证**：每次修改后需要测试出题功能是否正常

***

## 📝 总结

| 类别      | 文件数 | 需要修改     | 可以删除   | 保留不变 |
| ------- | --- | -------- | ------ | ---- |
| 核心出题文件  | 5   | 0        | 0      | 5    |
| 配置文件    | 2   | 0        | 0      | 2    |
| 数据文件    | 20+ | 16 (待升级) | 4 (临时) | 4    |
| 验证器文件   | 4   | 1        | 0      | 3    |
| 工具文件    | 3   | 1        | 0      | 2    |
| 临时/备份文件 | 8   | 0        | 7      | 1    |

