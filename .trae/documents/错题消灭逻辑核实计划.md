# 错题消灭逻辑核实计划

## 📊 用户需求确认

### 1. 手动消灭视为消灭

* ✅ 当前已实现

* 在错题库页面点击"手动消灭"按钮，错题会被标记为已消灭

### 2. 答对每周任务中同一变例的题目，视为消灭

* ❌ 当前未实现

* 需要添加自动消灭逻辑

***

## 🔍 当前代码分析

### 手动消灭逻辑（已实现）

**位置**：`src/components/weekly/ErrorLibrary.jsx` 第 80-87 行

```javascript
const handleResolveError = (errorId) => {
  setErrorNotebook(prev => prev.map(e => 
    e.id === errorId 
      ? { ...e, resolved: true, resolvedAt: new Date().toISOString() }
      : e
  ));
  setSelectedError(null);
};
```

### 自动消灭逻辑（待实现）

**位置**：`src/components/WeeklyMissionNew.jsx` 的 `handleSubmitAnswer` 函数

**需要添加的逻辑**：

1. 当用户答对题目时
2. 检查错题库中是否有相同变例的错题
3. 自动将该错题标记为已消灭

***

## ✅ 实施步骤

### 步骤 1：确保错题保存时记录完整信息

检查 `handleSubmitAnswer` 函数中保存错题时是否记录了 `specId` 和 `varId`。

### 步骤 2：添加自动消灭逻辑

在 `handleSubmitAnswer` 函数中，答对时添加自动消灭逻辑：

```javascript
if (aiResult.isCorrect) {
  // 答对时，自动消灭该变例的错题
  if (setErrorNotebook) {
    setErrorNotebook(prev => prev.map(e => 
      e.targetId === task.motifId 
      && e.specId === task.specId 
      && e.varId === task.varId 
      && !e.resolved
        ? { ...e, resolved: true, resolvedAt: new Date().toISOString() }
        : e
    ));
  }
}
```

### 步骤 3：添加消灭提示

在答题结果页面显示消灭信息：

* "✅ 恭喜！已自动消灭该变例的错题"

***

## 📋 需要修改的文件

1. `src/components/WeeklyMissionNew.jsx`

   * 在 `handleSubmitAnswer` 函数中添加自动消灭逻辑

2. `src/components/weekly/TaskDisplay.jsx`

   * 添加消灭提示的显示

