# 更新 AI 提示词计划

## 背景

用户提供了新版 AI 提示词，需要同步更新到代码中的三个位置。

## 需要修改的位置

### 1. `buildDNAProblem` 函数中的 `systemPrompt` (第 542 行)

**用途**: AI 调用时实际使用的提示词

**修改内容**:
- 替换整个 `# Role` 部分
- 添加 `# 核心使命` 部分
- 更新 `# 🎯 难度分级宪法` 部分
- 更新 `# 命题流程` 部分
- 更新 `# 强制学术约束` 部分
- 更新 `# 输出协议 (JSON Schema)` 部分

### 2. `useState` 中的 `systemPrompt` (第 1433 行)

**用途**: UI 显示的默认提示词（用于编辑器展示）

**修改内容**:
- 与位置 1 保持一致的内容

### 3. 重置默认按钮中的提示词 (第 2631 行)

**用途**: 点击"重置默认"按钮时设置的提示词

**修改内容**:
- 与位置 1 保持一致的内容

## 新版提示词核心变化

1. **新增 `# 核心使命` 部分**:
   - 立足广东（新高考 I 卷）
   - 借鉴强省（山东、浙江、湖北）
   - 追求结构识别、思想转化、隐含陷阱、分类完备性

2. **更新难度分级宪法**:
   - L2: 广东卷 T1/T2 风格
   - L3: 广东卷 T7/T8 或 T21(1) 风格
   - L4: 融合强省特色的压轴题

3. **更新命题流程**:
   - 逻辑锁定 (Logic Lock)
   - 变量旋钮 (Variable Knobs Activation)
   - 风格注入 (Style Injection)
   - 生成输出 (JSON Generation)

4. **更新输出协议**:
   - 新增 `difficulty` 字段
   - 新增 `linked_weapons` 字段

## 实施步骤

1. [ ] 更新 `buildDNAProblem` 函数中的 `systemPrompt`
2. [ ] 更新 `useState` 中的 `systemPrompt`
3. [ ] 更新重置默认按钮中的提示词
4. [ ] 运行 lint 检查确保无语法错误
