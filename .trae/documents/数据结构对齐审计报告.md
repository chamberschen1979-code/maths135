# 数据结构对齐审计报告

## 一、分析验证结果

### 用户分析的正确性评估

| 分析项 | 状态 | 说明 |
|--------|------|------|
| `knowledge-aggregation-index.json` 残留引用 | ✅ 正确 | 存在于 `questionGenerator.js` 和 `feedbackGenerator.js` |
| `formula_lib.json` 残留引用 | ❌ 已清理 | 之前的重构已完全移除 |
| `original_pool` 数据位置 | ✅ 正确 | 位于 `specialties -> variations -> original_pool` |
| `master_benchmarks` 数据位置 | ✅ 正确 | 位于 `specialties -> variations -> master_benchmarks` |
| `evolution_blueprint` 缺失 | ✅ 正确 | M01-M17.json 中确实没有此字段 |
| `system_instruction_template` 存在 | ⚠️ 部分正确 | 部分母题文件有，部分没有 |

### 当前数据结构（M05.json 示例）

```
M05.json
├── motif_id: "M05"
├── motif_name: "平面向量"
├── specialties[]
│   ├── spec_id: "V1"
│   ├── spec_name: "数量积的核心度量"
│   └── variations[]
│       ├── var_id: "1.1"
│       ├── name: "投影向量与夹角范围"
│       ├── original_pool[]        ← 题目池
│       ├── master_benchmarks[]    ← 标杆题
│       ├── variable_knobs         ← 变量旋钮
│       └── toolkit.linked_weapons ← 关联武器
```

### 残留问题定位

1. **questionGenerator.js** (第2行)：`import knowledgeAggregationIndex`
2. **feedbackGenerator.js** (第3行)：`import knowledgeAggregationIndex`
3. **WeeklyMission.jsx**：`buildDNAProblem` 函数中对 `evolution_blueprint` 的引用

---

## 二、修复计划

### 步骤 1：清理 questionGenerator.js 中的残留引用

- 移除 `knowledgeAggregationIndex` 导入
- 移除所有对该索引的引用
- 改为直接从 M01-M17.json 动态加载数据

### 步骤 2：清理 feedbackGenerator.js 中的残留引用

- 移除 `knowledgeAggregationIndex` 导入
- 移除所有对该索引的引用
- 改为从 strategy_lib.json 获取武器信息

### 步骤 3：优化 WeeklyMission.jsx 的 buildDNAProblem 函数

- 移除对 `evolution_blueprint` 的依赖（改为使用 `system_instruction_template`）
- 确保自动选择变例逻辑正常工作
- 使用 `master_benchmarks` 作为 Few-shot 示例

### 步骤 4：验证测试

- 运行 ESLint 检查
- 测试出题功能
- 确认无报错

---

## 三、风险评估

| 风险项 | 等级 | 缓解措施 |
|--------|------|----------|
| 删除 knowledgeAggregationIndex 后功能缺失 | 中 | 确保所有功能都有替代实现 |
| 部分母题缺少 system_instruction_template | 低 | 使用默认模板兜底 |
| 变例选择逻辑不完善 | 低 | 已添加自动选择逻辑 |

---

## 四、结论

用户的分析**基本正确**，但有一个细节需要澄清：

- `formula_lib.json` 的引用**已经完全清理**（之前的重构已完成）
- `knowledge-aggregation-index.json` 的引用**确实残留**在两个 utils 文件中
- 数据结构分析**完全正确**，`original_pool` 和 `master_benchmarks` 确实在新位置

建议执行上述修复计划，彻底完成数据结构的对齐。
